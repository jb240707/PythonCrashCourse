#Create a list of numbers from 1 to 1 mil, then use min and max to make sure your list actually starts at 1 and ends at 1 mil. 
#Also use sum function to see how quickly Python can add a mil numbers. 
numbers = list(range(1,1000001))
min(numbers)
max(numbers)
sum(numbers)